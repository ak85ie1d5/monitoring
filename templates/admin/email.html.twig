<p>Le domaine <a href="{{ site.url }}" title="{{ site.name }}" target="_blank">{{ site.url }}</a> à retourné un code HTTP <span style="color: red; font-weight: bold">{{ status.code }}</span> le {{ status.reportedAt | date('d/m/Y à H:i:s') }}</p>