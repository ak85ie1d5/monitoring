{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <style>
        .element {
            width: 33%;
        }

        .conteneur {
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;
        }
    </style>

    <div class="container">
        <div class="head text-center my-5">
            <h1>Accueil</h1>
            <h3>Votre site de monitoring en ligne</h3>
        </div>

        <div class="d-flex justify-content-around">
            <a class="btn btn-info" href="{{ path('analyze') }}">
                <i class="fas fa-tools"></i>
                Lancer le diagnostic
            </a>
            <a class="btn btn-warning" href="{{ path('clean') }}">
                <i class="fas fa-sync-alt"></i>
                Nettoyer l'historique
            </a>
        </div>

        <div class="conteneur my-5">
            {% for currentStatus in status %}
                <div class="element px-3">
                    <div class="card bg-success text-white mb-3 website-card">
                        <div class="card-header website-card-title">
                            <h4>{{ currentStatus.website.name }}</h4>
                            <small>
                                {{ currentStatus.website.url }}
                            </small>
                        </div>
                        <div class="card-body">
                            <div class="website-card-code">
                                HTTP: {{ currentStatus.code }}
                            </div>
                        </div>
                        <div class="card-footer">
                            <a href="{{ path('website_show', {'id': currentStatus.website.id}) }}" class="btn btn-lg btn-white">Voir la fiche</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
